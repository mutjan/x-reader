---
phase: 01-data-layer-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/news.py
  - src/processors/duplicate.py
  - src/processors/ai_processor.py
  - src/config/settings.py
  - src/utils/url.py (new)
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
requirements_note: DATA-01 and DATA-02 are already implemented and will be validated in this plan
must_haves:
  truths:
    - "URL规范化逻辑在整个系统中保持一致"
    - "生成AI提示时自动持久化预处理列表快照"
    - "多源RSS获取无数据丢失（验证现有功能）"
    - "处理前重复内容被正确移除（验证现有功能）"
  artifacts:
    - path: "src/utils/url.py"
      provides: "统一URL规范化工具函数"
      exports: ["normalize_url"]
    - path: "src/processors/ai_processor.py"
      provides: "快照生成功能"
      contains: "快照保存逻辑在process_batch方法中"
  key_links:
    - from: "news.py get_unique_id"
      to: "url.py normalize_url"
      via: "direct function call"
    - from: "duplicate.py deduplicate_raw"
      to: "url.py normalize_url"
      via: "direct function call"
    - from: "ai_processor.py process_batch"
      to: "snapshot_*.json"
      via: "JSON序列化"
---

<objective>
实现统一URL规范化和快照生成功能，验证现有数据层基础能力。

Purpose: 解决URL规范化不一致问题，建立快照机制基础，验证现有数据获取和去重功能
Output: 统一URL规范化工具，快照生成功能，DATA-01/DATA-02功能验证报告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-data-layer-stability/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 统一URL规范化逻辑</name>
  <files>src/utils/url.py, src/models/news.py, src/processors/duplicate.py</files>
  <action>
    1. 创建新文件 src/utils/url.py，实现统一的 normalize_url 函数
      - 移除URL中的锚点(#)
      - 移除URL中的跟踪参数(utm_*, ref等)
      - 统一HTTP/HTTPS协议
      - 移除末尾的斜杠
      - 处理www子域一致性
    2. 修改 src/models/news.py 中的 get_unique_id() 方法，调用 src.utils.url.normalize_url
    3. 修改 src/processors/duplicate.py 中的 deduplicate_raw() 方法，调用 src.utils.url.normalize_url
    4. 移除两个文件中重复的URL规范化逻辑

    确保所有URL规范化逻辑集中在一个地方，避免不一致。
  </action>
  <verify>
    <automated>
      python -c "
      from src.utils.url import normalize_url
      test_cases = [
        ('https://example.com/news/1?utm_source=twitter#title', 'https://example.com/news/1'),
        ('http://www.example.com/news/1/', 'https://example.com/news/1'),
        ('HTTPS://EXAMPLE.COM/NEWS/1', 'https://example.com/news/1'),
      ]
      for input_url, expected in test_cases:
        result = normalize_url(input_url)
        assert result == expected, f'{input_url} → {result} (expected {expected})'
      print('All URL normalization tests passed')
      "
    </automated>
  </verify>
  <done>URL规范化逻辑集中在src/utils/url.py，所有模块都调用同一个函数，测试用例全部通过</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 实现快照生成功能</name>
  <files>src/processors/ai_processor.py, src/config/settings.py</files>
  <action>
    1. 在 src/processors/ai_processor.py 的 ManualProcessor.process_batch() 方法中添加快照生成逻辑
      - 生成唯一的snapshot_id(8位MD5哈希)
      - 创建包含snapshot_id、创建时间、过滤参数、所有条目信息的快照对象
      - 将快照保存为JSON文件到临时目录，命名格式为 snapshot_{snapshot_id}.json
      - 在生成的AI提示文件名中包含snapshot_id: ai_prompt_{snapshot_id}.txt
      - 在日志中输出snapshot_id，提示用户导入时使用
    2. 在 src/config/settings.py 中添加 SNAPSHOT_DIR 配置项，默认使用临时目录
    3. 保留原有逻辑不变，只是增加快照保存步骤
  </action>
  <verify>
    <automated>
      python -c "
      import tempfile
      from src.processors.ai_processor import ManualProcessor
      from src.models.news import RawNewsItem
      processor = ManualProcessor()
      test_items = [
          RawNewsItem(url='https://example.com/1', title='Test 1', content='', source='test', published_at=datetime.now())
      ]
      processor.process_batch(test_items)
      # 检查是否生成了snapshot文件和prompt文件
      import os
      from src.config.settings import settings
      snapshot_files = [f for f in os.listdir(settings.SNAPSHOT_DIR) if f.startswith('snapshot_') and f.endswith('.json')]
      prompt_files = [f for f in os.listdir(settings.SNAPSHOT_DIR) if f.startswith('ai_prompt_') and f.endswith('.txt')]
      assert len(snapshot_files) > 0, 'No snapshot file generated'
      assert len(prompt_files) > 0, 'No prompt file generated'
      # 检查snapshot_id是否匹配
      snapshot_id = snapshot_files[0].split('_')[1].split('.')[0]
      assert any(snapshot_id in f for f in prompt_files), 'Snapshot ID not in prompt filename'
      print('Snapshot generation test passed')
      "
    </automated>
  </verify>
  <done>生成AI提示时自动保存快照文件，快照ID包含在提示文件名中，日志输出快照ID</done>
</task>

</tasks>

<verification>
## 整体验证步骤
### 现有功能验证 (DATA-01 + DATA-02)
1. 验证多源RSS获取：运行 `python main.py --time-window 1h --dry-run`，检查日志确认所有配置的源都被正确抓取，无数据丢失错误
2. 验证重复移除功能：运行 `python -c "from src.processors.duplicate import deduplicate_raw; from src.models.news import RawNewsItem; items = [RawNewsItem(url='https://example.com/1', title='Test 1', content='', source='test', published_at=datetime.now()), RawNewsItem(url='https://example.com/1?utm_source=test', title='Test 1', content='', source='test2', published_at=datetime.now())]; result = deduplicate_raw(items); assert len(result) == 1, 'Duplicate not removed'; print('Duplicate removal test passed')"`

### 新功能验证
3. 运行完整流水线：`python main.py --time-window 24h --skip-ai`
4. 检查生成的snapshot_*.json文件和ai_prompt_*.txt文件，确认内容一致
5. 验证URL规范化在所有模块中一致
</verification>

<success_criteria>
- URL规范化测试用例全部通过
- 运行流水线后生成快照文件和带snapshot_id的提示文件
- 多源RSS获取无数据丢失错误
- 重复内容被正确移除
- 所有模块使用同一个URL规范化函数
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-layer-stability/01-01-SUMMARY.md`
</output>
