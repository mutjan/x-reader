---
phase: 01-data-layer-stability
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/processors/ai_processor.py
  - scripts/import_results.py
autonomous: true
requirements: [DATA-04, DATA-01, DATA-02, DATA-03]
requirements_note: Final validation of all phase 1 requirements
must_haves:
  truths:
    - "AI结果匹配完全基于URL主键，无索引回退逻辑"
    - "导入AI结果时使用快照数据而非重新获取"
    - "超过7天的快照文件自动清理"
    - "AI结果匹配100%准确，不受feed更新影响"
  artifacts:
    - path: "src/processors/ai_processor.py"
      provides: "严格URL匹配逻辑，快照清理功能"
      contains: "parse_response函数无索引回退"
    - path: "scripts/import_results.py"
      provides: "基于快照的AI结果导入流程"
      contains: "加载快照文件逻辑"
  key_links:
    - from: "import_results.py"
      to: "snapshot_*.json"
      via: "JSON反序列化"
      pattern: "load_json.*snapshot_"
    - from: "ai_processor.py parse_response"
      to: "original_items"
      via: "URL exact match"
      pattern: "url_to_item\\[original_url\\]"
---

<objective>
实现严格URL匹配和快照导入流程，解决AI结果匹配错位问题。

Purpose: 完全消除因feed更新导致的AI结果匹配错误，确保输入输出一致性
Output: 严格URL匹配逻辑，基于快照的导入流程，自动快照清理机制
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-1-data-layer-stability/RESEARCH.md
@.planning/phase-1-data-layer-stability/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 移除索引回退，实现严格URL匹配</name>
  <files>src/processors/ai_processor.py</files>
  <action>
    1. 修改 src/processors/ai_processor.py 中的 parse_response() 方法
      - 构建URL到原始条目的字典映射(url_to_item)
      - 移除所有基于索引匹配的逻辑
      - 仅使用URL进行精确匹配，匹配失败时记录错误并跳过该条目
      - 确保AI返回结果中包含original_url字段，与原始条目URL完全匹配
    2. 修改 build_prompt() 方法，确保输出到AI的每个条目都包含original_url字段
  </action>
  <verify>
    <automated>
      python -c "
      from src.processors.ai_processor import ManualProcessor
      from src.models.news import RawNewsItem
      processor = ManualProcessor()
      test_items = [
          RawNewsItem(url='https://example.com/1', title='Test 1', content='', source='test', published_at=datetime.now()),
          RawNewsItem(url='https://example.com/2', title='Test 2', content='', source='test', published_at=datetime.now()),
      ]
      # 测试正常匹配
      response_json = json.dumps([
          {'original_url': 'https://example.com/1', 'score': 8, 'reason': 'Test'},
          {'original_url': 'https://example.com/2', 'score': 5, 'reason': 'Test'}
      ])
      results = processor.parse_response(response_json, test_items)
      assert len(results) == 2, f'Expected 2 results, got {len(results)}'
      # 测试URL不匹配时跳过
      response_json = json.dumps([
          {'original_url': 'https://example.com/1', 'score': 8, 'reason': 'Test'},
          {'original_url': 'https://example.com/nonexistent', 'score': 5, 'reason': 'Test'}
      ])
      results = processor.parse_response(response_json, test_items)
      assert len(results) == 1, f'Expected 1 result for missing URL, got {len(results)}'
      print('Strict URL matching test passed')
      "
    </automated>
  </verify>
  <done>parse_response()方法完全基于URL匹配，无索引回退逻辑，URL不匹配时直接跳过并记录错误</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修改导入脚本使用快照数据</name>
  <files>scripts/import_results.py</files>
  <action>
    1. 修改 scripts/import_results.py 脚本
      - 添加snapshot_id命令行参数
      - 如果提供了snapshot_id，从快照文件加载原始条目列表
      - 如果没有提供snapshot_id，提示用户使用snapshot_id并退出(保持向后兼容可选)
      - 移除原有重新获取数据、去重、过滤的流程
      - 使用加载的快照条目调用parse_response()进行匹配
    2. 更新脚本帮助信息，说明snapshot_id参数的使用方法
  </action>
  <verify>
    <automated>
      python scripts/import_results.py --help | grep -q "snapshot_id" && echo "snapshot_id parameter exists"
      # 测试快照加载功能
      python -c "
      import json
      import tempfile
      from src.config.settings import settings
      # 创建测试快照
      test_snapshot = {
          'snapshot_id': 'test1234',
          'created_at': datetime.now().isoformat(),
          'items': [
              {'url': 'https://example.com/1', 'title': 'Test 1', 'content': '', 'source': 'test', 'published_at': datetime.now().isoformat()}
          ]
      }
      snapshot_file = os.path.join(settings.SNAPSHOT_DIR, 'snapshot_test1234.json')
      with open(snapshot_file, 'w') as f:
          json.dump(test_snapshot, f)
      # 测试导入脚本加载快照
      import subprocess
      result = subprocess.run(
          ['python', 'scripts/import_results.py', '--snapshot-id', 'test1234', '--dry-run'],
          capture_output=True, text=True
      )
      assert 'Loaded 1 items from snapshot' in result.stdout, 'Snapshot not loaded correctly'
      print('Import script snapshot loading test passed')
      "
    </automated>
  </verify>
  <done>导入脚本支持--snapshot-id参数，加载快照数据进行匹配，不再重新获取数据</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 添加快照自动清理机制</name>
  <files>src/processors/ai_processor.py</files>
  <action>
    1. 在process_batch()方法保存新快照后，添加快照清理逻辑
      - 扫描快照目录中所有snapshot_*.json文件和对应的ai_prompt_*.txt文件
      - 删除创建时间超过7天的旧快照和提示文件
    2. 确保清理逻辑不会删除正在使用的快照文件
  </action>
  <verify>
    <automated>
      python -c "
      import os
      import time
      from src.config.settings import settings
      from src.processors.ai_processor import ManualProcessor
      # 创建旧的测试快照
      old_snapshot = {'snapshot_id': 'old12345', 'created_at': (datetime.now() - timedelta(days=8)).isoformat(), 'items': []}
      old_file = os.path.join(settings.SNAPSHOT_DIR, 'snapshot_old12345.json')
      with open(old_file, 'w') as f:
          json.dump(old_snapshot, f)
      # 修改文件时间为8天前
      old_time = time.time() - 8 * 24 * 3600
      os.utime(old_file, (old_time, old_time))
      # 运行process_batch触发清理
      processor = ManualProcessor()
      processor.process_batch([])
      # 检查旧文件是否被删除
      assert not os.path.exists(old_file), 'Old snapshot not cleaned up'
      print('Snapshot cleanup test passed')
      "
    </automated>
  </verify>
  <done>超过7天的快照文件自动清理，避免磁盘空间占用</done>
</task>

</tasks>

<verification>
## 整体验证步骤
1. 运行完整流水线：`python main.py --time-window 24h`
2. 记录生成的snapshot_id
3. 模拟feed更新（修改源配置或添加新条目）
4. 运行导入脚本：`python scripts/import_results.py --snapshot-id {snapshot_id} --result-file tests/fixtures/test_ai_result.json --dry-run`
5. 验证所有AI结果都正确匹配到原始条目，无错位或匹配失败
6. 检查日志中无"URL matching failed, using index matching"警告
7. 验证旧快照清理功能正常
</verification>

<success_criteria>
- 严格URL匹配测试通过，无索引回退逻辑
- 导入脚本使用快照数据，无需重新获取
- 即使feed在提示生成和导入之间更新，匹配结果100%准确
- 超过7天的快照文件自动清理
- 所有Phase 1要求全部满足
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-layer-stability/01-02-SUMMARY.md`
</output>
