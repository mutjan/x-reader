---
phase: 11-event-table-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/app.py
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - API返回的事件数据包含主新闻的摘要和扩展性字段
    - 事件数据包含last_seen_at作为更新时间
    - 单条新闻事件和多新闻事件在API层区分标识
  artifacts:
    - path: src/web/app.py
      provides: /api/events端点扩展
      exports: ["main_news_summary", "main_news_expansion", "is_single_news"]
  key_links:
    - from: /api/events
      to: event_groups.json + news_data.json
      via: 关联查询主新闻数据
---

<objective>
扩展后端 /api/events API，为事件表格展示提供必要的数据字段

Purpose: 支持前端事件表格展示摘要、扩展性和更新时间
Output: 扩展后的API返回包含主新闻摘要、扩展性、更新时间等字段
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-event-table-refactor/11-CONTEXT.md
@src/web/app.py

## 现有API结构
当前 `/api/events` 返回的事件对象：
```python
{
    'event_id': str,
    'title': str,
    'max_grade': str,
    'max_score': int,
    'start_time': str,
    'end_time': str,
    'news_count': int,
    'entities': list,
    'news_list': list  # 完整新闻列表
}
```

## 需要新增的字段
根据 D-04 决策：
- `main_news_summary`: 主新闻（最高评分）的摘要
- `main_news_expansion`: 主新闻的扩展性
- `last_seen_at`: 事件更新时间（使用 end_time 已有字段）
- `is_single_news`: 是否为单条新闻事件（news_count === 1）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 /api/events 端点返回字段</name>
  <read_first>
    - src/web/app.py (第728-879行，/api/events端点实现)
  </read_first>
  <files>src/web/app.py</files>
  <action>
    修改 src/web/app.py 中 `/api/events` 端点的事件对象构建逻辑（约第769-781行）。

    在构建 event 对象时，添加以下字段：
    1. 找出主新闻：在 event_news 列表中找出 score 最高的新闻（如果评分相同，取 published_at 最新的）
    2. 添加 `main_news_summary`: 主新闻的 summary 字段，如果不存在则返回空字符串
    3. 添加 `main_news_expansion`: 主新闻的 expansion 字段，如果不存在则返回空字符串
    4. 添加 `is_single_news`: 布尔值，当 len(event_news) === 1 时为 True
    5. `last_seen_at` 字段已存在（使用 end_time 的值）

    修改后的 event 对象结构：
    ```python
    event = {
        'event_id': group.get('group_id', group.get('event_id', '')),
        'title': group.get('event_title', group.get('title', '未命名事件')),
        'max_grade': group.get('max_grade', 'B'),
        'max_score': group.get('max_score', 0),
        'start_time': group.get('first_seen_at', group.get('start_time', '')),
        'end_time': group.get('last_seen_at', group.get('end_time', '')),
        'last_seen_at': group.get('last_seen_at', group.get('end_time', '')),  # 明确添加
        'news_count': len(event_news),
        'entities': group.get('entities', []),
        'news_list': event_news,
        'main_news_summary': main_news.get('summary', '') if main_news else '',
        'main_news_expansion': main_news.get('expansion', '') if main_news else '',
        'is_single_news': len(event_news) == 1
    }
    ```
  </action>
  <verify>
    <automated>curl -s "http://localhost:8081/api/events?page=1&page_size=5" | python3 -c "import sys,json;d=json.load(sys.stdin);e=d['data'][0] if d['data'] else {};print('main_news_summary' in e and 'main_news_expansion' in e and 'is_single_news' in e)"</automated>
  </verify>
  <done>API返回包含 main_news_summary、main_news_expansion、is_single_news 字段</done>
</task>

</tasks>

<verification>
- [ ] /api/events 返回的事件对象包含 main_news_summary 字段
- [ ] /api/events 返回的事件对象包含 main_news_expansion 字段
- [ ] /api/events 返回的事件对象包含 is_single_news 布尔字段
- [ ] 主新闻选择逻辑正确（取评分最高，同分取最新）
</verification>

<success_criteria>
1. API返回的事件数据包含主新闻摘要和扩展性字段
2. 单条新闻事件正确标记 is_single_news: true
3. 向后兼容，不影响现有前端功能
</success_criteria>

<output>
After completion, create `.planning/phases/11-event-table-refactor/11-01-SUMMARY.md`
</output>
